demo.direct:
  pattern:   /user/edit/direct
  defaults:  { _controller: AlterwayDemoBundle:Test:directProblem, _format: json }
  
demo.user:
  pattern:   /user/edit/{id}
  defaults:  { _controller: AlterwayDemoBundle:Test:userWithoutAnnotate, _format: json }
  
